compile:
gcc -lpthread mandel.c -o mandel -Wall
get the console:
sudo fgconsole // returns integer x
sudo chvt (x+1) // login to console + 1
login as momefilo and run programm:
mandelbrodt/mandel
to log out run:
sudo chvt x // console



git init
git add -A
git remote add origin https://github.com/momefilo/mandelbrodt.git
git remote set-url origin git@github.com:momefilo/mandelbrodt.git
git branch -M main
git push -u origin main
git commit -a -m "mein kommentar"
git commit --amend -m "mein kommentar"
git push origin master
git pull origin master

guielement:
90 Grad im Uhrzeigersinn drehen
dann Horizon spiegeln
